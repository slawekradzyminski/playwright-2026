# Copy this file to .env before running the tests.
# .env is ignored by git so credentials stay on your machine.

# Shared training application (no Docker required):
APP_BASE_URL=https://aitesters.byst.re

# For the local Docker stack, use this instead:
# APP_BASE_URL=http://localhost:8081

ADMIN_USERNAME=admin
ADMIN_PASSWORD=LocalDemoAdmin123!
