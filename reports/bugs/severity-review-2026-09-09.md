# Severity reassessment — 2026-09-09

Reviewed all 32 existing reports against affected journey, scope, persistent effects, recovery and evidence limits. Existing observations and statuses are retained; this is not new runtime reproduction. Categories remain unchanged. Potential consequences are distinguished from observed outcomes. The rationale is presented before the selected level.

| Bug | Impact rationale and limits | Previous | Selected |
| --- | --- | --- | --- |
| [BUG-001]([L][F]-BUG-001-signup-overlong-username-validation.md) | Signup rejects the overlong username with 400, but describes the minimum instead of maximum length. The defect misdirects correction of one invalid input; account creation and the length boundary remain protected. No persistent mutation or blocked valid signup is recorded. | Low | **Low** |
| [BUG-002]([M][F]-BUG-002-product-update-blank-fields.md) | An administrator can persist a blank product name/category through update although creation rejects those values. This degrades stored catalog information, but the record does not demonstrate lost inventory or blocked purchasing. An administrator can correct the fields; whether blanks are allowed on update remains unresolved. | Medium | **Medium** |
| [BUG-003]([L][D]-BUG-003-product-delete-error-body.md) | Deleting an already absent product returns the correct 404 status but omits the documented error body. The caller can determine the absent-resource outcome from that status without retrying a destructive action. A consumer parsing failure is possible but unobserved; no blocked deletion or state corruption is demonstrated. | Medium | **Low** |
| [BUG-004]([M][D]-BUG-004-product-description-contract.md) | Product creation rejects an empty description that the published schema permits. Contract-following callers cannot create that payload, but supplying a nonempty description gives a clear correction path. This is a request-contract incompatibility; the evidence does not establish that all product creation fails. | Medium | **Medium** |
| [BUG-005]([M][D]-BUG-005-qr-error-response-content-type-contract.md) | QR validation and authentication errors are JSON while the contract declares PNG, so the advertised decoding format is wrong on normal failure paths. Consumers need a media-type/error-handling correction to expose those responses. Successful QR generation is unaffected; no generated-client outage was exercised. | Medium | **Medium** |
| [BUG-006]([M][D]-BUG-006-cart-error-response-contract.md) | Cart validation/authentication error objects do not match the advertised CartDto across several operations. Contract-based integrations lack the correct error model for recovery, although runtime rejections protect the cart. Integration handling requires an explicit correction; an actual client crash or checkout outage was not observed. | Medium | **Medium** |
| [BUG-007]([M][D]-BUG-007-cart-stock-conflict-undocumented.md) | Cart add/update correctly reject insufficient stock and preserve both customers' carts, but the contract omits the 409 recovery branch. Integrators cannot derive this supported stock-conflict outcome from the declared operation. No stock corruption or successful-purchase failure is demonstrated; clients can add explicit 409 handling. | Medium | **Medium** |
| [BUG-008]([M][F]-BUG-008-product-delete-referenced-by-cart.md) | An ordinary customer cart causes administrator product deletion to return 500, so retirement of that product cannot complete while the reference exists. Clearing the owned cart enabled deletion in the reproduction, but that is not necessarily an acceptable workaround for other customers' carts. Impact is a conditional admin operation; no wider outage or corrupt state was observed. | Medium | **Medium** |
| [BUG-009]([H][F]-BUG-009-order-reopening-inventory.md) | A cancelled order was persisted as PAID while its restored stock remained available. This breaks consistency between active orders and inventory and creates a concrete route to overselling. Actual overselling was not executed, but correcting a display or retrying the request does not repair the recorded business-state inconsistency. | High | **High** |
| [BUG-010]([M][D]-BUG-010-auth-orders-error-contract.md) | Authentication lifecycle and order errors are declared as successful token/user/order DTOs. The contract therefore gives integrations incorrect models for authentication and order recovery. Correct runtime rejection is preserved, and no consumer outage is demonstrated; the material impact is the incompatible error interface, not a proven authentication bypass. | Medium | **Medium** |
| [BUG-011]([M][F]-BUG-011-order-invalid-status-unauthorized.md) | Malformed order status is reported as 401 even though the administrator token remains valid and a valid update succeeds. Callers cannot use the status to distinguish input correction from authentication recovery. Reauthentication loops are a possible consequence, not observed evidence; correcting the payload restores the supported operation. | Medium | **Medium** |
| [BUG-012]([M][D]-BUG-012-checkout-stock-conflict-undocumented.md) | Checkout correctly rejects insufficient stock with 409 and preserves cart, stock and order state, but the contract omits this expected checkout recovery branch. Integrations need explicit handling to guide quantity correction. This impairs the declared purchase interface without demonstrating lost orders, stock or payment. | Medium | **Medium** |
| [BUG-013]([L][F]-BUG-013-order-mutation-stale-updated-at.md) | Mutation and readback timestamps differ by milliseconds while business fields and separately verified stock effects agree. The record demonstrates a metadata discrepancy, not lost updates or incorrect order state. A readback supplies persisted metadata; intended timestamp precision/flush semantics remain unresolved. | Low | **Low** |
| [BUG-014]([M][F]-BUG-014-inventory-malformed-request-id-401.md) | A malformed inventory requestId returns an authentication error for a valid administrator without recording an adjustment. The response hides the input-repair category and can misdirect API recovery; token expiry or a retry loop was not demonstrated. Correcting the UUID is a recovery path, and the precise public error mapping remains under triage. | Medium | **Medium** |
| [BUG-015]([M][D]-BUG-015-inventory-invalid-query-and-path-error-schema.md) | The inventory contract omits or misrepresents validation, missing-item, stock-conflict and access-error responses across four operations. Integrations cannot model those supported branches correctly from it. Runtime rejection and stock protection were verified; no stock corruption or consumer outage was demonstrated. | Medium | **Medium** |
| [BUG-020]([M][D]-BUG-020-user-read-error-schemas.md) | Both account-read operations return authentication error objects while declaring a user DTO or list. Contract-based consumers need a different model to expose authentication failures, the same interface incompatibility identified in cart and prompt reports. Runtime access remains protected; an actual generated-client failure is untested. | Low | **Medium** |
| [BUG-021]([M][D]-BUG-021-user-edit-error-schemas.md) | Account-edit validation and access errors are documented as UserEntity rather than field/error objects. Integrators lose the declared models needed to explain field correction or denied access, as in the comparable cart error-schema finding. Runtime rejections remain correct; no account corruption or consumer outage was observed. | Low | **Medium** |
| [BUG-022]([L][F]-BUG-022-cart-read-after-account-deletion-500.md) | A cart read with a deleted account's old token returns 500 instead of a controlled rejection. The account and its data were deleted successfully; the evidence shows no access to retained data, effect on active customers or failed deletion. Impact is misleading diagnostics for an already unusable identity, recoverable by ending that stale session; the intended rejection status remains unresolved. | Medium | **Low** |
| [BUG-027]([M][D]-BUG-027-prompt-error-schema-mismatch.md) | Prompt operations advertise prompt DTOs for authentication errors but return error objects. Contract-based consumers need corrected error models, consistently with account and cart findings. Authentication is still rejected correctly and valid prompt operations are not shown to fail; an actual client outage was not tested. | Medium | **Medium** |
| [BUG-028]([H][F]-BUG-028-register-sign-in-creates-account.md) | Selecting Sign in from a valid registration form submits signup and creates a persistent account, verified by subsequent successful login. The navigation action reserves username/email without the user choosing Create account; navigating back does not undo that mutation. This compromises user control over account creation. The user explicitly confirmed this severity on 2026-09-09. | High | **High** |
| [BUG-029]([L][UI]-BUG-029-mobile-toasts-cover-footer-actions.md) | A settled mobile toast overlaps secondary footer actions, but it has a visible close control and practical interaction blockage has not been established. The recorded effect is temporary visual obstruction, not persistent loss of registration access. Placement intent remains unresolved, so the report stays suspected. | Low | **Low** |
| [BUG-030]([M][UI]-BUG-030-homepage-mobile-welcome-clipping.md) | The narrow homepage hides portions of identity, introduction and shortcut descriptions inside an overflow-hidden panel at repeated widths. This is actual loss of readable content, not merely different spacing; scrolling the document does not reveal the clipped portion. Navigation remains clickable and wider layouts are readable, limiting the demonstrated scope. | Medium | **Medium** |
| [BUG-031]([L][UI]-BUG-031-mobile-menu-logout-alignment.md) | Logout is centered while adjacent account links are left-aligned. The record shows a presentation inconsistency without unreadable text, blocked activation or unexpected session behavior. No recovery action is needed to use the menu. | Low | **Low** |
| [BUG-032]([L][UI]-BUG-032-mobile-menu-footer-overlap.md) | Opening the mobile menu near the footer partially obscures its heading and makes the visual boundary unclear. The record does not establish blocked interaction, and closing the menu exposes the underlying content. Whether this overlay is unintended remains unresolved. | Low | **Low** |
| [BUG-033]([M][UI]-BUG-033-product-category-badge-clipping.md) | Long category text is clipped beyond product card boundaries at all sampled widths, without an explicit truncation treatment. Users cannot read the full badge in those cards; core purchasing is not shown to be blocked. The loss of content is confirmed, while the frequency of equally long real categories is unknown. | Medium | **Medium** |
| [BUG-034]([H][A]-BUG-034-product-details-keyboard-inaccessible.md) | Keyboard traversal skips the product title/image action and reaches quantity/cart controls instead; the clickable card has no keyboard interaction. Keyboard-only customers cannot open product details through the catalog journey. Mouse access is not an equivalent workaround for that affected group; the main information-access route is blocked. | High | **High** |
| [BUG-035]([M][F]-BUG-035-product-search-surrounding-spaces.md) | An existing product is returned for its name but disappears from search results when the same name has surrounding spaces. This gives a misleading empty result in a user-confirmed supported input case. Removing the spaces restores results; product data and purchasing outside that search are not shown to be broken. | Medium | **Medium** |
| [BUG-036]([M][UX]-BUG-036-product-categories-push-search-below-fold.md) | With ten categories, search and products are pushed below the initial viewport at several widths, contrary to the confirmed discoverability requirement. Users must scroll past a substantial filter list before browsing. Scrolling still reaches the controls; there is no demonstrated complete browsing block or measured latency defect. | Medium | **Medium** |
| [BUG-037]([L][F]-BUG-037-empty-catalog-category-count.md) | An empty mocked catalog is summarized as having one category while the category panel correctly reports none. This creates contradictory summary information, but no products or categories are actually fabricated in the list. Evidence covers simulated empty/loading/error states, not a corrupted backend catalog. | Low | **Low** |
| [BUG-038]([L][UI]-BUG-038-qr-mobile-image-distortion.md) | The square QR image is squeezed horizontally at 360px and more at 320px. The 360px screenshot still decodes to the submitted content, so current evidence establishes visual distortion rather than failure of the primary QR task. Decoding at 320px and with physical scanners was not established; that limitation does not prove failure. | Low | **Low** |
| [BUG-039]([M][A]-BUG-039-cart-link-accessible-name.md) | The shared cart link is focusable but has no accessible name in five scanned states, so assistive-technology users cannot identify its purpose from the link semantics. This materially impairs navigation. Activation failure or absence of every alternative cart route was not established, so a complete cart-access block is not claimed. | Medium | **Medium** |
| [BUG-040]([M][A]-BUG-040-profile-invalid-field-semantics.md) | Invalid email feedback is visible and focus moves to the field, but the input exposes neither its invalid state nor an error association. This impairs accessible correction of the form. A role=alert message exists and actual screen-reader behavior was not tested, so the record does not establish that correction is impossible. | Medium | **Medium** |

Result: 0 Critical, 3 High, 20 Medium, 9 Low.
