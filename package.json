{
  "name": "test-automation-in-practice-2026",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test tests/api/",
    "test:ui": "playwright test tests/ui/",
    "coverage:api": "node scripts/api-coverage.mjs",
    "coverage:api:check": "node scripts/api-coverage.mjs --check"
  },
  "keywords": [
    "playwright",
    "testing",
    "typescript",
    "automation"
  ],
  "author": "",
  "license": "ISC",
  "description": "A TypeScript Playwright suite for validating the API and UI layers of the local gateway-first training stack",
  "engines": {
    "node": ">=24 <25"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.6.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.13.3",
    "jsqr": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
